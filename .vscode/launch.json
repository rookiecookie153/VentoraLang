{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Ventora",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/bin/ventora.exe",
            "args": ["src/test.vt"],    // Optional default script
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "preLaunchTask": "Build Ventora",  // This runs 'make' before launching
            // "console": "integratedTerminal"
        }
    ]
}
